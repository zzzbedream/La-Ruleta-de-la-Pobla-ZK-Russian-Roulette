{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd zk-mafia-frontend && bun install && bun run build",
  "outputDirectory": "zk-mafia-frontend/dist",
  "installCommand": "bun install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
